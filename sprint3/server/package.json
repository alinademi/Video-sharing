{
  "name": "server",
  "version": "1.0.0",
  "description": "This is a video platform that runs on express server",
  "main": "server.js",
  "dependencies": {
    "concurrently": "^5.2.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "uuid": "^8.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm start --prefix ../client",
    "server": "nodemon server.js",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "Ali Nademi",
  "license": "ISC"
}
